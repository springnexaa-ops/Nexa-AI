{
  "name": "nexa-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check:public-js": "node --check public/app-functional.js && node --check public/auth-fetch.js && node --check public/auth-widget.js && node --check public/nexa-shell.js && node --check public/nexa-chat-bridge.js && node --check public/scroll-fix.js && node --check public/file-upload-fix.js && node --check public/nexa-5d-ui.js",
    "test:medical-audit": "tsx tests/medical-audit.test.ts",
    "test": "npm run typecheck && npm run check:public-js && npm run test:medical-audit",
    "cf:dry-run": "wrangler deploy --dry-run",
    "production:gate": "npm test && npm run cf:dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "tsx": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
